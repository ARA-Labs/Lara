# Corpus unit stochastic-interpolants/C01 — gap (mandatory assumptions CQ unmet)
#
# Source: m0/annotations/stochastic-interpolants.yaml C01 (descriptive, analytic_proof;
# corpus pin 62e9b54). Headline: quadratic regression remains valid for dependent couplings —
# Theorem 3.1 states b_t(x) = E(I'_t | I_t=x) uniquely minimizes the quadratic objective L_b for
# ANY coupling with the correct marginals (including dependent ones) and yields the transport
# equation; E01 checks the transport-equation consequence on three GMM couplings.
#
# Lowering notes (conventions: corpus-units/LOWERING.md):
#   * analytic_proof -> analytic_argument(P, Prf), conclusion holds_analytically(P). This is one
#     of the 6 strict-flavored annotations (strict_or_defeasible: strict); per LOWERING it still
#     lowers through the defeasible analytic_argument — the strict proof-assistant adapter does
#     not exist yet, so migration to a strict rule + certificate is future work.
#   * analytic_argument's mandatory assumptions_hold CQ (assumptions_established) maps onto the
#     annotation's mandatory "is Theorem 3.1's proof available/checkable within the artifact, or
#     only cited?" (and "is the unique-minimizer property itself checked, not only the transport
#     equation?") — both annotated unmet-gap (the theorem is attested via trace N02; neither its
#     hypotheses nor its proof are reproduced). An open mandatory obligation is IncompleteArgument,
#     so NO complete arg is declarable: E2-style gap.
#   * scope_licensed (mandatory) is annotated met: E01 verifies the transport equation for the
#     independent, conditional-xi, and data-dependent couplings, so the "dependent couplings"
#     scope is licensed on the tested instances; its leaf is supplied. Regularity/integrability
#     hypotheses are optional-unmet.
#   * Coverage flag (annotation): the "ANY joint density with correct marginals" universal has no
#     direct construct; it is instantiable per coupling and the universal is carried by an
#     attested external proof — recorded here per the kind-(a) binding-discipline convention.
#
# Expected verdict is at the bottom of this file (golden oracle).

artifact stochastic-interpolants at sha256:73bd49c9083c
policy corpus-v1
use backends [nd@1]

# --- The headline claim, an attested analytic result -------------------------------------------

claim c01
  nl      = "Theorem 3.1 states the conditional expectation b_t = E(I prime_t given I_t=x) uniquely minimizes the quadratic regression objective for any coupling with the correct marginals, including dependent couplings, and yields the transport equation; the theorem is only cited, its proof and hypotheses are not reproduced, and E01 checks only the transport-equation consequence on three GMM couplings"
  formal  = holds_analytically(quadratic_regression_minimizer)
  binding = { author = m0-annotator, audit-status = unreviewed }

# --- Evidence leaves are present ... ------------------------------------------------------------

# Principal premise: Theorem 3.1 as attested in trace N02 (proof text not in the artifact).
leaf e1 : derives(theorem_3_1, quadratic_regression_minimizer)
  kind       = attested
  provenance = ai-executed
  refs       = [logic/claims.md#C01, trace/exploration_tree.yaml#N02, section-3.1]

leaf e2 : scope_licensed(quadratic_regression_minimizer)
  kind       = attested
  provenance = ai-executed
  refs       = [logic/experiments.md#E01-Procedure-step-2, logic/experiments.md#E01-Procedure-step-4]

# --- ... but no complete argument is declarable -------------------------------------------------

# (Intentionally no `arg`: assumptions_established(theorem_3_1) has no honest leaf — the artifact
# reproduces neither the theorem's hypotheses nor its proof, and E01 verifies only the transport
# equation, not the minimizer property — and analytic_argument makes that obligation mandatory.
# The complete-support set is therefore empty.)

status c01

# ==============================================================================================
# EXPECTED VERDICT (golden oracle)
# ==============================================================================================
# Compiled AF: no arguments.
# Claim status:
#   c01 -> gap (empty complete support; statusC = Gap iff null claimSupport, spec §8)
