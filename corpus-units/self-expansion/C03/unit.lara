# Corpus unit self-expansion/C03 — gap (policy-mandatory variance CQ unmet)
#
# Source: m0/annotations/self-expansion.yaml C03 (causal, component_ablation; corpus pin 62e9b54).
# Headline: on-demand (z-score-triggered) self-expansion outperforms static and per-task expansion
# -- full SEMA's cells strictly exceed all five ablation variants (No Exp., Avg. W., Rand. W.,
# Top-1 Sel., Rand. Sel.) on both ImageNet-A and VTAB across A_N and A_bar (E03, Table 2).
#
# Lowering notes (conventions: corpus-units/LOWERING.md):
#   * E03 Setup/Procedure specify no seed count or variance reporting -- single point estimates,
#     with sub-1% VTAB margins (Avg. W. A_N 90.84 vs SEMA 91.26). corpus-v1's mandatory
#     variance_reported CQ has no discharging leaf, so no complete component_ablation instance is
#     declarable: E2-style, claim lands `gap` on empty complete support (as adaptive-pruning/C03).
#   * Divergence recorded (uniform-standard rule): the annotator rated variance MANDATORY here too
#     and marked it unmet-gap, so no policy-vs-annotation conflict on the decisive CQ. Met CQs:
#     single_variable <- CQ2 (variants differ only in the ablated component, same backbone /
#     hyperparameters / splits); protocol_parity <- CQ1 (all five variants trained + evaluated on
#     both datasets under the stated procedure). The optional trigger-isolation CQ (isolate the
#     z-score trigger vs any expansion trigger, via E02/Table 5 outside C03's proof set) is
#     unmet-gap but optional -- no status effect.
#   * Strict-flavored dominance certifier (table-dominance) is future work; lowered defeasible.
#     Q = incremental_accuracy; the per-cell figures live in nl and refs. All three dead ends
#     (N03 fixed-single-adapter, N06 hard-RD routing, N12 average/random weighting) are rejected
#     alternatives realized as C03's own ablation rows -- evidence, not attacks.
#
# Expected verdict is at the bottom of this file (golden oracle).

artifact self-expansion at sha256:aa759bc10299
policy corpus-v1
use backends [nd@1]

# --- The headline claim, stated but not assemblable at corpus-v1's evidential bar --------------

claim c03
  nl      = "On-demand z-score-triggered self-expansion outperforms static and per-task expansion: full SEMA strictly exceeds all five ablation variants (No Exp., Avg. W., Rand. W., Top-1 Sel., Rand. Sel.) on both ImageNet-A and VTAB across A_N and A_bar"
  formal  = contributes(on_demand_expansion, sema, incremental_accuracy)
  binding = { author = m0-annotator, audit-status = unreviewed }

# --- Evidence leaves are present ... ------------------------------------------------------------

# Principal premise: the Table 2 ablation cells (full SEMA vs the five variants on both datasets,
# both metrics), per result cell in the annotation and folded into refs.
leaf e1 : reports(e03, ablation_effect(on_demand_expansion, sema, incremental_accuracy, positive))
  kind       = observed
  provenance = ai-executed
  refs       = [logic/claims.md#C03, evidence/tables/table2_ablation_expansion_composing.md, logic/experiments.md#E03-Procedure-steps-2-3, trace/exploration_tree.yaml#N02-N12]

leaf e2 : isolated(on_demand_expansion, e03)
  kind       = attested
  provenance = ai-executed
  refs       = [logic/experiments.md#E03-Setup, logic/experiments.md#E03-Procedure-step-2]

leaf e3 : matched_protocol(e03)
  kind       = attested
  provenance = ai-executed
  refs       = [logic/experiments.md#E03-Procedure-step-2, logic/experiments.md#E03-Procedure-step-3]

# --- ... but no complete argument is declarable -------------------------------------------------

# (Intentionally no `arg`: variance_reported(e03) has no honest leaf -- E03 reports single-run
# point estimates -- and corpus-v1 makes that obligation mandatory. Complete support is empty.)

status c03

# ==============================================================================================
# EXPECTED VERDICT (golden oracle)
# ==============================================================================================
# Compiled AF: no arguments.
# Claim status:
#   c03 -> gap (empty complete support; statusC = Gap iff null claimSupport, spec §8)
