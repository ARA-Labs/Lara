# Mechanical reviewer — corpus claim-support verdicts as reviews

Deterministically generated by `scripts/render-reviews.hs` from the checker's
own per-claim verdicts over the frozen 60-unit corpus sample
(`corpus-units/MANIFEST.tsv`, seed 20260801). NOT an LLM,
and no new trusted code: this is untrusted presentation over the accept-path
located diagnostics (`Lara.MechReview`). One review comment per
non-`justified` claim, in manifest order; a `justified` claim raises no
comment. The status the checker computes IS the review content — a `gap` names
its unmet obligation (or empty support), a `defeated` names the attacking
argument that sinks the claim, a `contested` names the unresolved conflict.

**51 review comments** — 48 gap, 3 defeated.

## adaptive-pruning.C03 — gap

Claim `c03`: `better(apt, prune_distill_cofi, train_cost_at_parity, roberta_mnli_sst2)`

*Stated:* APT achieves accuracy on par with Prune+Distill (CoFi) on MNLI/SST2 at 60% sparsity on RoBERTa-base (within 0.9 points) with 41.6% of its training memory and 2.5x faster 97% TTA

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## adaptive-pruning.C04 — gap

Claim `c04`: `contributes(kurtosis_salience, apt_llama2_7b, openllm_avg)`

*Stated:* Removing the kurtosis (outlier-aware) salience term from APT drops the LLaMA2-7B four-task OpenLLM average from 50.0 to 38.1 at 30% sparsity, a 23.8% relative drop, so the kurtosis term is critical for large-LM pruning

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## all-in-one.C01 — gap

Claim `c01`: `better(simformer_dense, npe, c2st, benchmark_tasks)`

*Stated:* Simformer-dense reaches C2ST closer to 0.5 than NPE on 3 of 4 benchmark tasks (Gaussian Mixture, Two Moons, SLCP) at all simulation budgets, tying on Gaussian Linear except at 1e4 sims

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## all-in-one.C03 — gap

Claim `c03`: `generalizes_to(accurate_conditional_estimation, arbitrary_conditionals)`

*Stated:* Simformer accurately estimates all arbitrary conditionals of the joint: mean C2ST over 100 sampled conditionals stays below 0.7 on Tree, HMM, Two Moons, and SLCP at 1e5 sims

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## all-in-one.C05 — gap

Claim `c05`: `holds(interval_conditioning, simformer)`

*Stated:* Guided diffusion (Algorithm 1, c(x)=x-u, s(t)=1/sigma(t)^2) lets Simformer condition on intervals at inference without retraining: constrained Hodgkin-Huxley posterior-predictive energy stays below the 10% quantile threshold and voltage traces stay consistent with observations

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## all-in-one.C06 — gap

Claim `c06`: `not_better(all_conditionals, posterior_only, posterior_c2st, benchmark_tasks)`

*Stated:* The all-in-one (all-conditionals) training objective does not hurt posterior-only accuracy: all-conditionals Simformer posterior C2ST is comparable to the posterior-only variant (which matches the NPSE baseline) across benchmark tasks, and helps on SLCP

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## bam.C06 — gap

Claim `c06`: `contributes(batch_size, bam, convergence_speed)`

*Stated:* BaM converges faster at larger batch sizes (dose levels up to B ~= D) in gradient evaluations, while ADVI and GSM show marginal or no improvement from larger batches

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## bbox.C02 — gap

Claim `c02`: `better(bbox_adapter, azure_sft, training_cost, strategyqa_gsm8k)`

*Stated:* BBOX-ADAPTER full-step training costs $3.48 (StrategyQA) and $11.58 (GSM8K) vs Azure SFT $153.00 and $216.50, an approximately 31.30x mean cost reduction under one gpt-3.5-turbo-1106 pricing snapshot; A100 adapter compute is never dollarized

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## bbox.C04 — gap

Claim `c04`: `contributes(nce_loss, bbox_adapter, reasoning_accuracy)`

*Stated:* Ranking-based NCE loss beats MLM loss for the BBOX-ADAPTER by up to +10.10 points (StrategyQA 0.1B: 71.62 vs 61.52) and +3.05 (GSM8K 0.3B: 73.86 vs 70.81), same datasets and adapter sizes, single run per cell

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## bbox.C05 — gap

Claim `c05`: `better(bbox_adapter, unadapted_target, transfer_accuracy, new_black_box_llms)`

*Stated:* The frozen BBOX-ADAPTER trained on gpt-3.5-turbo plugs into davinci-002 (+6.85 avg over 3 datasets) and Mixtral-8x7B (+4.50) with no retraining; per-dataset cells and a sampling-matched control are absent

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## bbox.C06 — gap

Claim `c06`: `not_better(ai_feedback, ground_truth_labels, reasoning_accuracy, bbox_datasets)`

*Stated:* Selecting positive samples via a GPT-4 judge (no ground-truth labels) is competitive with ground-truth selection: max accuracy gap 1.77 points over 4 datasets, well under the 5-point falsification margin; no variance reported

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## bridging-data-gaps.C03 — gap

Claim `c03`: `better(ddpm_tan, full_finetune, training_resource_cost, ffhq_sunglasses_10shot)`

*Stated:* DDPM-TAN dominates full fine-tuning on FFHQ to 10-shot Sunglasses: 1.3% vs 100% trained params, 300 vs ~5000 iters, 3 vs 7.5 GPU h, 9 vs 20 GB peak, at equal-or-better FID; the batch 40 / 8x A100 vs batch 5 / 1 GPU configs are never normalized

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## bridging-data-gaps.C05 — gap

Claim `c05`: `contributes(adversarial_noise, ddpm_tan, fid_improvement)`

*Stated:* Along the TAN ablation chain (adaptor-only 41.88, +similarity guidance 26.41, +adversarial noise 20.06 FID on FFHQ to 10-shot Sunglasses at 300 iters), adversarial noise adds a 6.4-point drop and similarity guidance a larger 15.5-point drop; single run per variant

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## fre.C03 — gap

Claim `c03`: `performs(fre_all, gc_baselines, cross_family_return)`

*Stated:* A single frozen FRE-all agent beats GC-IQL/GC-BC on non-goal-reaching families (ant-directional 55.2 vs 4.8/6.5; kitchen 66 vs 3/1) while staying competitive on goal-reaching (ant-goal 48.8 vs 0.0); the GC conditioning protocol on non-goal tasks is unspecified

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## fre.C04 — defeated

Claim `c04`: `contributes(reward_diversity, fre_all, antmaze_total_score)`

*Stated:* On AntMaze, performance scales smoothly with reward diversity and FRE-all dominates all reward-subset ablations — total score FRE-all 47.3 +- 7 is the argmax over the 7 variants

> The claim's only support `a1` is labelled `out`: the undercut `d1` (challenging the `variance_reported` critical question of `a1`) is unattacked and defeats `a1` — status **defeated**.

## ftrl.C01 — gap

Claim `c01`: `not_better(vanilla_ft, from_scratch, final_return, rl_envs)`

*Stated:* Vanilla RL fine-tuning fails to leverage pre-trained capabilities: due to forgetting (FPC) its final performance is comparable to or worse than training from scratch across NetHack, Montezuma, and RoboticSequence (NetHack ~1K vs frozen ~5K return; RoboticSequence FAR stages 0% within 100K steps)

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## lbcs.C03 — gap

Claim `c03`: `holds_analytically(lbcs_eps_convergence)`

*Stated:* Theorem 2: under Conditions 1 (Progressable) and 2 (Stable Moving), LBCS converges eps-optimally, with f2(mt) reaching within eps of f2* almost surely as t -> infinity; the artifact corroborates only empirically via the E03 search-time plateau and E06 joint reduction

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## lbcs.C05 — gap

Claim `c05`: `better(lbcs, probabilistic, top5_accuracy, imagenet1k)`

*Stated:* At ImageNet-1k, LBCS attains the highest Top-5 accuracy among {Uniform, EL2N, GraNd, Moderate, CCS, Probabilistic, LBCS} at each predefined ratio, and its optimized selection ratio falls below the predefined one (68.53 vs 70, 77.82 vs 80)

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## mechanistic-understanding.C02 — gap

Claim `c02`: `better(subtract_toxic, no_op, toxicity_at_matched_ppl, gpt2_medium)`

*Stated:* Subtracting a toxic vector (wtoxic, mlp_v19, or svd_utoxic0) from the last-layer residual stream reduces Perspective toxicity below No Op (0.453 -> 0.245/0.305/0.268) while preserving quality at DPO-matched perplexity (ppl 23.30-23.56 vs DPO 23.34; F1 ~0.193)

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## mechanistic-understanding.C04 — gap

Claim `c04`: `holds(minimal_weight_change, gpt2_dpo)`

*Stated:* Every matched GPT2-medium vs GPT2DPO parameter tensor changes minimally: cosine similarity above 0.99 and average norm difference below 1e-5, with the unembedding the sole exception at below 1e-3; MLP kToxic and vToxic fall inside the same bounds

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## pinn.C03 — gap

Claim `c03`: `better(residual_component, ic_bc_components, ill_conditioning, pinn_pdes)`

*Stated:* The residual loss component (the differential-operator term) is the dominant source of ill-conditioning: for convection, reaction, and wave the residual-component Hessian max eigenvalue exceeds the comparatively well-conditioned IC and BC components, and theory predicts the operator D is the source

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## pinn.C05 — gap

Claim `c05`: `better(adam_lbfgs, adam, min_l2re, pinn_pdes)`

*Stated:* Adam+L-BFGS consistently outperforms Adam or L-BFGS alone: min L2RE 14.2x smaller than Adam on convection and 6.07x smaller than L-BFGS on wave, and the lowest min loss and L2RE per (PDE, width) cell, except reaction at width 100 where Adam is comparable

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## robust-clip.C02 — gap

Claim `c02`: `better(fare, tecoa, embedding_preservation, imagenet_val)`

*Stated:* FARE-CLIP preserves the original CLIP embedding structure better than TeCoA at matched training radius: mean clean embedding loss FARE2 32.7 vs TeCoA2 236.9 and FARE4 47.6 vs TeCoA4 292.7; mean adversarial loss at eps 4/255 FARE2 103.9 vs TeCoA2 301.9 and FARE4 81.9 vs TeCoA4 335.0

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## robust-clip.C03 — gap

Claim `c03`: `performs(fare, original_clip, targeted_attack_robustness)`

*Stated:* FARE makes LVLMs fully robust against stealthy targeted linf attacks: FARE4 has 0/150 attack successes at both eps 2/255 and 4/255 (0 percent), and FARE2 is 0 percent at 2/255 and 2.0 percent at 4/255, each cell a mean over 6 targets x 25 images; original CLIP is 100 percent under the same protocol

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## robust-clip.C07 — gap

Claim `c07`: `better(nonrobust_shared_clip, robust_clip, transfer_vulnerability, coco_captioning)`

*Stated:* Transfer attacks between LVLMs that share a non-robust CLIP encoder are effective but fail with robust encoders: OF-CLIP adversarial COCO on LLaVA-CLIP drops CIDEr to 25.5 vs clean 115.5 and LLaVA-CLIP on OF-CLIP to 8.3 vs clean 79.7, while transfer onto robust FARE targets stays near-clean (LLaVA-FARE2 115.9, OF-FARE4 79.9)

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## sapg.C01 — gap

Claim `c01`: `contributes(parallel_env_scaling, ppo, performance_saturation)`

*Stated:* PPO asymptotic performance saturates as parallel environments increase (plateau around 25k envs), due to redundant IID samples from a shared Gaussian policy; the sweep spans ~1500 to 100000 environments (4x beyond the threshold) on Shadow Hand and Allegro Kuka Throw over >= 3 seeds, and SAPG exceeds the PPO plateau on the same tasks

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## sapg.C02 — gap

Claim `c02`: `better(sapg, dexpbt, asymptotic_success, allegro_kuka_hard)`

*Stated:* SAPG achieves significantly higher asymptotic performance than DexPBT (by 12 to 66 percent) on the hard AllegroKuka dexterous manipulation suite at 2e10 steps, with PPO and PQL near-zero on the same tasks; all methods run at N=24576 envs, 16-step horizon, 5 seeds with mean +- stderr

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## sapg.C06 — gap

Claim `c06`: `contributes(follower_entropy_reg, sapg, reorientation_performance)`

*Stated:* Follower entropy regularization (sigma=0.005) improves SAPG on the harder exploration task Reorientation by up to 16.5 percent over sigma=0, while sigma=0 is optimal on the easier AllegroHand, Regrasping, and Throw; only sigma varies across {0, 0.003, 0.005} with a sigma=0 baseline on every task, but no per-seed variance is reported

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## self-composing-policies.C04 — gap

Claim `c04`: `better(componet, prognet, inference_time_scaling, tasks_up_to_300)`

*Stated:* CompoNet empirical inference time grows substantially slower than ProgressiveNet as tasks increase from 1 to 300 (CompoNet gradual, ProgressiveNet clearly quadratic), on identical hardware with a 1-minute / at-least-40-inference averaging protocol and batch size 8 for both models

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## self-expansion.C03 — gap

Claim `c03`: `contributes(on_demand_expansion, sema, incremental_accuracy)`

*Stated:* On-demand z-score-triggered self-expansion outperforms static and per-task expansion: full SEMA strictly exceeds all five ablation variants (No Exp., Avg. W., Rand. W., Top-1 Sel., Rand. Sel.) on both ImageNet-A and VTAB across A_N and A_bar

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## self-expansion.C04 — gap

Claim `c04`: `contributes(functional_adapter, sema, incremental_accuracy)`

*Stated:* SEMA generalises across functional adapter types: swapping the default Adapter for LoRA or Convpass inside the unchanged SEMA framework keeps A_N within about 2% on both ImageNet-A and VTAB (gaps 1.03 and 1.05 on ImageNet-A; LoRA +0.59 and Convpass -0.58 on VTAB)

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## semantic-self-consistency.C03 — gap

Claim `c03`: `better(embedding_outlier_removal, sc_baseline, best_accuracy, model_dataset_conditions)`

*Stated:* Embedding-based outlier removal (Isolation Forest, kNN, OCSVM) before the self-consistency vote improves or matches the SC baseline in the majority of the 45 method-model-dataset cells of Table 2, with gains persisting at reduced sample sizes

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## semantic-self-consistency.C05 — gap

Claim `c05`: `correlated(avg_seq_length, accuracy_increase, model_dataset_pairs)`

*Stated:* Average reasoning-chain sequence length positively correlates with the average accuracy increase from the semantic methods over the 15 model-dataset pairs of Table 3 (recomputed Pearson r ~= 0.242, not significant at n = 15)

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## semantic-self-consistency.C06 — gap

Claim `c06`: `not_correlated(avg_bleu, accuracy_increase, model_dataset_pairs)`

*Stated:* Average BLEU score does not correlate with the average accuracy increase from the semantic methods over the 15 model-dataset pairs of Table 3 (recomputed Pearson r ~= -0.234, not significant at n = 15)

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## sequential-neural-score-estimation.C04 — gap

Claim `c04`: `better(tsnpse_vp, tsnpe, valid_summary_stats, pyloric_budget_le_200k)`

*Stated:* TSNPSE-VP achieves a superior percentage of valid summary statistics on the Pyloric problem, reaching 81% in the final round and exceeding both TSNPE and SNVI at every matched simulation budget below 200k

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## stay-on-topic-with-classifier-free-guidance.C02 — gap

Claim `c02`: `not_better(cfg, vanilla_2x_flops, zero_shot_accuracy, lm_eval_9task)`

*Stated:* On 5 of 9 benchmarks ANCOVA at p=0.01 finds no significant accuracy difference between CFG models at doubled inference FLOPs and vanilla models at equal FLOPs (of the 4 significant tasks 2 favor CFG and 2 favor vanilla), read as matching about 2x model size; no equivalence test or power analysis is reported

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## stay-on-topic-with-classifier-free-guidance.C05 — gap

Claim `c05`: `preferred(cfg_negative_prompting, vanilla, system_prompt_following)`

*Stated:* At gamma=3 blind paired human evaluators prefer CFG-with-negative-prompting completions for system-prompt following 75 percent of the time over vanilla, while user-prompt relevance preference stays near 52 percent; per-gamma vote counts and rater variability for the 75 and 52 figures are not reported and gamma=3 is the post hoc peak

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## stay-on-topic-with-classifier-free-guidance.C06 — gap

Claim `c06`: `holds(cfg_lowers_sampling_entropy, falcon7b)`

*Stated:* Falcon-7b-Base with CFG at gamma=1.5 has mean logit entropy 4.7 vs 5.4 for vanilla on P3 samples and its top-p=0.9 vocabulary overlap with Falcon-7b-Instruct is below 30 percent, so CFG lowers entropy yet differs from instruction tuning; no significance test is run and only one model pair is measured

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## stochastic-interpolants.C01 — gap

Claim `c01`: `holds_analytically(quadratic_regression_minimizer)`

*Stated:* Theorem 3.1 states the conditional expectation b_t = E(I prime_t given I_t=x) uniquely minimizes the quadratic regression objective for any coupling with the correct marginals, including dependent couplings, and yields the transport equation; the theorem is only cited, its proof and hypotheses are not reproduced, and E01 checks only the transport-equation consequence on three GMM couplings

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## stochastic-interpolants.C03 — gap

Claim `c03`: `better(dependent_coupling, uncoupled_baseline, fid, imagenet256_inpainting)`

*Stated:* On ImageNet-256 inpainting the data-dependent coupling reaches FID-50k 1.13 vs 1.35 for the uncoupled interpolant baseline under a matched U-Net and training budget; no seeds or FID variability are reported, the baseline evaluation protocol is unstated, and known-pixel compositing is not separated from the coupling effect

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## test-time-model-adaptation.C04 — defeated

Claim `c04`: `contributes(entropy_only_fitness, cma_es_tta, adaptation_collapse)`

*Stated:* Entropy-only fitness cannot provide stable CMA-ES learning signals for online TTA: norm-layer + entropy collapses to 0.1% and prompts + entropy reaches 44.9% vs NoAdapt 55.5% on ImageNet-C severity 5

> The claim's only support `a1` is labelled `out`: the undercut `d1` (challenging the `single_variable` critical question of `a1`) is unattacked and defeats `a1` — status **defeated**.

## test-time-model-adaptation.C05 — gap

Claim `c05`: `contributes(activation_discrepancy_fitness, cma_es_prompt_tta, adaptation_accuracy)`

*Stated:* On ImageNet-C severity 5 the activation-discrepancy fitness lifts CMA-ES prompt adaptation from 55.5 percent NoAdapt to 63.4 percent alone and 66.3 percent with activation shifting (best of all FOA variants), contrasting the entropy-only failure; stability is inferred from endpoint accuracy only, with no seeds or variance reported

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## test-time-model-adaptation.C07 — gap

Claim `c07`: `better(foa, sar, adaptation_accuracy, non_iid_shift)`

*Stated:* Under non-i.i.d. test streams FOA beats SAR and TENT: online imbalanced label shift 62.1 percent vs SAR 60.8 and TENT 60.2, mixed domain shift 62.0 percent vs SAR 61.4 and TENT 56.9, with lower ECE; baseline configuration under shift is unstated and the mixed stream is a single unseeded interleaving

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## what-will-my-model-forget.C01 — gap

Claim `c01`: `better(representation_with_prior, fixed_logit, forecasting_f1, forgetting_configs)`

*Stated:* Representation-with-prior forecasting attains the highest binary F1 in 7 of 8 Table 1 (model, dataset, tuning) configurations, the lone exception being fixed-logit 68.37 vs 67.81 on FLAN-T5Large head tuning; no seeds or uncertainty are reported for the Table 1 cells so the per-column argmax rests on single runs

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## what-will-my-model-forget.C03 — gap

Claim `c03`: `not_better(trainable_logit, threshold, forecasting_f1, flan_t5_configs)`

*Stated:* Trainable logit-change forecasting beats the tuned threshold on BART0Large Full FT (57.15 vs 55.75) but loses on FLAN-T5Large LoRA (36.54 vs 43.93) and Full FT (40.91 vs 48.43), a partially interpretable method that fails to transfer to FLAN-T5; the deciding cells are single runs and the kernel-capacity explanation is not diagnosed

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## rebench-fix_embedding.C12 — gap

Claim `c12`: `holds(timeout_checkpoint_pathology, fix_embedding_scoring)`

*Stated:* The scorer 180 s hard timeout interacts pathologically with large checkpoints: a 1.56B-param 3.1 GB fp16 .pth spends about 60 s just deserialising, timeouts cascade within a run (run 11 six in a row), and run 5 computed score 1.291 was discarded as invalidSubmission

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## rebench-nanogpt_chat_rl.C04 — gap

Claim `c04`: `holds_analytically(tournament_judge_call_optimal)`

*Stated:* Pairwise single-elimination tournament selection is judge-call-optimal for best-of-N: at N=8 it uses N-1 = 7 judge calls per prompt vs 28 for round-robin, with ceil(log2 N) = 3 dependent latency rounds under parallel dispatch

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## rebench-restricted_mlm.C07 — gap

Claim `c07`: `code_property(tao_solution, division_outside_forward)`

*Stated:* The single use of division lives outside the forward pass: forward() of ConvMLMWithBiBigrams normalises only by multiplying with the inverse_stds buffer, and the EMA division 1/std sits in update_inverse_stds, called only from the training loop after optimizer.step

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## rebench-rust_codecontests.C09 — gap

Claim `c09`: `code_property(rust_codecontests_solver, repair_planned_not_shipped)`

*Stated:* Candidate-repair was planned but not implemented: notes.md line 22 lists 'Try doing repair on attempts that compiled' under Things to try now, but the shipped solve_code_contests_rust.py has no code path feeding evaluator failure output back for a second attempt

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## rebench-triton_cumsum.C05 — gap

Claim `c05`: `performs(superblock_atomic_pipeline, rebench_reference, triton_cumsum_score)`

*Stated:* The superblock + atomic-suffix 3-kernel Triton pipeline achieves the reference score 0.47 (1.6 ms) on N=1e8 int32 on H100, but the score is transcribed from the README and no scorer run of the shipped kernel is recorded in the artifact

> No argument assembles the unit's evidence into a checked support for this claim; its complete-support set is empty, so at the `corpus-v1` evidential bar the claim is unsupported — status **gap**.

## rebench-triton_cumsum.C09 — defeated

Claim `c09`: `holds(bandwidth_bound_floor, pytorch_two_cumsum)`

*Stated:* The pure-PyTorch two-cumsum pipeline is memory-bandwidth-bound and cannot beat the reference: eager variants cluster at 2.2-5.0 ms and torch.compile plateaus at 1.5-1.8 ms on H100 at N=1e8, above the 0.47 reference, because each invocation moves about 1.6 GB of HBM traffic

> The claim's only support `a1` is labelled `out`: the undercut `d1` (challenging the `coverage` critical question of `a1`) is unattacked and defeats `a1` — status **defeated**.

