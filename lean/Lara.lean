-- LARA mechanized reference semantics — library root.
-- Currently: the frozen nf/≡ carve-out (spec §9 result 11 / claim C01).
-- To come (post-M1 freeze): the backend interface + ND adapter (results 8,10),
-- support terms + dependency accountability (result 3), compilation + grounded
-- least-fixpoint (results 4,5), backend replacement (result 9). See
-- docs/mechanization-plan.md.
import Lara.Prop
