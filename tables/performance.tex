% Generated by scripts/bench.hs (make bench) at 318088c -- do not edit.
\begin{table}
\caption{Production-checker performance over the 60 frozen corpus
units and the 420-record harness, on one machine:
Apple M5 Pro, \qty{64}{\giga\byte} RAM,
darwin/aarch64, GHC~9.14.1,
Lean 4.32.0.
Haskell times are in-process medians of 5
~sections of 100~batched runs each; end-to-end is
decode + check + verdict render; parse is the marginal decode cost
(end-to-end minus the check-plus-render section over the pre-decoded
input), and each later phase section forces that stage's output with
earlier stages pre-forced, so phases need not sum exactly. The Lean
reference driver is timed as a full subprocess (startup and decode
included), a different protocol
reported only to show why it stays the test oracle rather than the
shipped checker; no cross-driver ratio is meaningful.}
\label{tab:performance}
\small
\begin{tabular}{@{}lrr@{}}
\toprule
 & median & worst \\
\midrule
Corpus unit, end-to-end (\si{\micro\second}) & \num{671.5} & \num{773.2} \\
\quad parse (\si{\micro\second}) & \num{662.4} & \num{755.8} \\
\quad check + render, pre-decoded (\si{\micro\second}) & \num{9.0} & \num{17.8} \\
\quad validate (\si{\micro\second}) & \num{0.3} & \num{0.4} \\
\quad check (\si{\micro\second}) & \num{1.0} & \num{16.4} \\
\quad compile (\si{\micro\second}) & \num{0.5} & \num{1.9} \\
\quad evaluate (\si{\micro\second}) & \num{1.3} & \num{2.6} \\
Per-artifact total (27 artifacts, \si{\micro\second}) & \num{1370.6} & \num{2679.8} \\
Framework size (nodes / edges) & 0 / 0 & 2 / 1 \\
Lean reference driver, subprocess (\si{\milli\second}) & \num{2.8} & \num{3.2} \\
\midrule
420-record harness, one full pass (\si{\milli\second}) & \multicolumn{2}{r}{\num{243.3}} \\
\bottomrule
\end{tabular}
\end{table}
